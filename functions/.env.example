# Firebase Cloud Functions Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit the actual .env file to version control!

# =============================================================================
# WOMPI PAYMENT GATEWAY SECRETS
# =============================================================================
# These are PRIVATE keys that should ONLY exist on the backend server
# NEVER expose these in your client-side code!

# Wompi Integrity Secret - Used to generate payment signatures
# Get this from your Wompi dashboard
WOMPI_INTEGRITY_SECRET=your_integrity_secret_here

# Wompi Events Secret - Used to verify webhook signatures
# Get this from your Wompi dashboard
WOMPI_EVENTS_SECRET=your_events_secret_here

# Wompi Private Key - Used for API calls to Wompi (if needed)
# Get this from your Wompi dashboard
WOMPI_PRIVATE_KEY=your_private_key_here

# =============================================================================
# HOW TO SET THESE IN FIREBASE
# =============================================================================
# DO NOT use a .env file in production!
# Instead, set these as Firebase Function environment variables:
#
# firebase functions:config:set \
#   wompi.integrity_secret="your_secret" \
#   wompi.events_secret="your_events_secret" \
#   wompi.private_key="your_private_key"
#
# To view current config:
# firebase functions:config:get
#
# To deploy with new config:
# firebase deploy --only functions
#
# =============================================================================
# SECURITY NOTES
# =============================================================================
# 1. The .env file is for LOCAL DEVELOPMENT ONLY
# 2. The functions/.gitignore already ignores .env files
# 3. In production, Firebase Functions uses functions:config:set
# 4. NEVER commit secrets to Git
# 5. NEVER expose these secrets in client code
# 6. Rotate these secrets regularly for security
# =============================================================================
